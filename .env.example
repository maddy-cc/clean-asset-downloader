# 复制为 .env 后按需修改；不要把 .env 提交到 Git。

# 可选：从浏览器登录 douyin.com 后，在 DevTools → Network 任意请求里复制 Cookie 整段。
# 配置后可走网页端详情接口，解析与浏览器 Network 一致的最高清档位（如 qs=15 / 更高 br）。
# DOUYIN_COOKIE=ttwid=...; msToken=...; ...

# 可选：限制代理预览和下载体积，单位 byte。
MAX_PROXY_BYTES=20971520
MAX_DOWNLOAD_BYTES=314572800
